# Production Environment Configuration for VPS (178.32.191.152)

# Application Settings
APP_NAME="ETL Showcase"
APP_VERSION="2.0.0"
DEBUG=false

# API Settings - Bind to all interfaces for VPS access
API_HOST=0.0.0.0
API_PORT=8001

# CORS Settings - Allow Vercel frontend
CORS_ORIGINS=["https://etl-pipeline-snowy.vercel.app","https://etl-pipeline-snowy.vercel.app/","http://localhost:3000","http://localhost:5173"]

# Database Settings (Update these with your actual VPS database credentials)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=student1
DB_USER=student1
DB_PASSWORD=learnDB

# Pool Settings for Production
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=30
DB_POOL_TIMEOUT=30

# Pipeline Settings
PIPELINE_BATCH_SIZE=1000
PIPELINE_MAX_WORKERS=4
DATA_QUALITY_THRESHOLD=0.95

# Monitoring
ENABLE_PERFORMANCE_MONITORING=true
LOG_LEVEL=INFO
ENABLE_STRUCTURED_LOGGING=true

# Security
SECRET_KEY=your-production-secret-key-change-this

# File Paths
DATA_INPUT_PATH=data/input
DATA_PROCESSED_PATH=data/processed
DATA_DEADLETTER_PATH=data/deadletter
SCHEMA_REGISTRY_PATH=schemas